<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<title>留言板</title>
<%=stylesheet_link_tag "article_template1"%>
</head>
  
<body>
<div id="bg">
<div class="frame">
<div id="head"><%=infoNavi%></div>
<div id="center">
  <table>
    <tr>
      <td id="menu" rowspan="2">
        <h1 style="color:#fff;margin-left:50px;">留言板</h1>
		</td>
      <td id="line"></td>
      <td rowspan="2" id="main">
          <div><%=flash[:comment_flash]%></div>
          <p><%=link_to '添加新留言', :action => 'create', :id => @cinema_id %></p>
          <% @comments.each do |comment| t="#{comment.title}"%>
          <% if((h t).index(/\[replay\]/)) %>
          <ul class="replay">
            <li><b class="comment_title"><%="#{(h t).sub(/\[replay\]/, '<font color=red>管理员回复</font>')}" %></b>&nbsp;
            <%=h comment.created_at.to_s.to(18)%></li>
          </ul>
          <div class="replay_content"><%=h comment.content%></div>
          <% else %>
          <ul class="comment">
            <li><b class="comment_title"><%="#{(h t).sub(/\[replay\]/, '<font color=red>管理员回复</font>')}" %></b>&nbsp;
            <%=h comment.created_at.to_s.to(18)%></li>
          </ul>
          <div class="comment_content"><%=h comment.content%></div>
          <% end %>

          <% end %>
	 </td>
    </tr>
    <tr>
      <td id="line2"></td>
    </tr>
  </table>
</div>
<!--<div id="navigate"></div>-->
<div id="link" align="center"><%=link%></div>
<div id="footer"></div>
</div>
</div>
</body>
</html>
